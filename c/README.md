# C 예제 모음 (merge/check 용 빠른 가이드)

이 폴더는 `python/` 학습 예제를 C로 옮긴 버전입니다.

## 빠른 검증 (권장)

```bash
bash c/build_all.sh
```

- 모든 `c/**/*.c` 파일을 한 번에 컴파일합니다.
- 기본 출력 경로는 `/tmp/cbuild`이며, 인자로 변경할 수 있습니다.

```bash
bash c/build_all.sh /tmp/my_c_build
```
